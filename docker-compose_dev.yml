services:
  cgdf_api:
    container_name: cgdf_dev
    build:
      context: .
      dockerfile: Dockerfile_dev
    ports:
      - "${APP_PORT:-55555}:${APP_PORT:-55555}"
    environment:
      - APP_PORT=${APP_PORT:-55555}
    volumes:
      - .:/app  